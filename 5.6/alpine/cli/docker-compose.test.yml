---
version: "3.5"
services:
  sut:
    build:
      context: ../../../
    volumes:
      - ../../../tests:/tmp/tests
    command: |
      sh -c "for FILE in /tmp/tests/*.sh; do exec $${FILE}; done"
