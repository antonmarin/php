---
version: "3.5"
services:
  sut:
    build:
      context: ../../../
      dockerfile: ${PWD}/Dockerfile
    volumes:
      - ../../../tests:/tmp/tests
    command: |
      sh -c "for FILE in /tmp/tests/*.sh; do exec $${FILE}; done"
